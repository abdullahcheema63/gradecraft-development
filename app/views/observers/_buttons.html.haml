- content_for :context_menu do
  .context-menu
    %ul
      - if current_user_is_admin? || current_course.is_licensed?
        = active_course_link_to decorative_glyph(:plus) + "New Observer", new_user_path, class: "button button-edit"
