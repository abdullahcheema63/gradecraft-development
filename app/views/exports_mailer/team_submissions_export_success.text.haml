Good news, #{ @professor.first_name }!

Your requested submissions export of #{ @course.assignment_term.downcase} "#{ @assignment.name }" in course #{ @course.name } was successfully created for #{ @course.team_term.downcase } "#{ @team.name }."

You can download your completed ZIP archive using this link below:
#{ secure_download_url(@secure_token) }.
This link will expire after one week.

You can also download the submissions archive from the submissions downloads page here: #{ downloads_url }.

Sincerely,
GradeCraft
